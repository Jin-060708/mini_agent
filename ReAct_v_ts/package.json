{
  "name": "react-agent-ts",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "npm run build && node dist/src/main.js",
    "server": "npm run build && node dist/src/agent.js",
    "test": "npm run build && node --test dist/tests/checkpointResume.test.js dist/tests/toolConcurrency.test.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "dotenv": "^18.0.3",
    "expr-eval": "^2.0.2",
    "openai": "^7.23.0",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@types/node": "^26.6.2",
    "typescript": "^7.0.2"
  }
}
